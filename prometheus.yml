global:
  scrape_interval: 15s # 15초마다 데이터 수집

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: /actuator/prometheus # 이 경로에서 수집
    static_configs:
      # ✅ 수집할 대상 지정
      # ( 호스트의 9090 포트를 바라봄)
      - targets: ['studylink-backend:9090']